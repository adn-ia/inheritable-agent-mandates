node_modules/
_to_delete/
.env
.env.local
.env.*.local
data/
*.key
*.pem
secrets/
*.log
.DS_Store

# internes — pas pour le public
DOSSIER.md
perplexity_feedback_dossier.md
publication/PUBLIER-le-repo.md

# lancement — local seulement
publication/
notes/

# dossier de travail interne : protocoles, rapports de banc, brouillons
_local/

# documents personnels — jamais dans un dépôt public
*.pdf
# …sauf les explications grand public, qui sont publiées volontairement
# (liées depuis le README ; sans cette ligne, un futur PDF y serait bloqué en silence)
!vulgarise/*.pdf

# rendus page par page du PDF déjà suivi : dérivés, rien d'original à versionner
vulgarise/Une-IA-en-laisse_FR_p*.png

# artefacts de compilation / déploiement
build/

# clone de la source ERC-8312 pour le test de couture — jamais committé
integration/bounded-agent-actions/
integration/out/
integration/cache/

# caches Python
__pycache__/

# mesure de frontière contre l'implémentation de référence ERC-8226 :
# gardée en réserve (décision du 13/08/2026), pas publiée
interop/erc8226-frontiere/

# V4 du mandat : version écrite le 14/08 avec un renew() libre, écartée par Helmy
# le jour même (elle relâche une clause). À réécrire avec le plafond de naissance
# et les périodes fixes, après les réponses du fil. NE PAS DÉPLOYER TELLE QUELLE.
contracts/InheritableAgentMandateV4.sol
